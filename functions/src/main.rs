fn main() {
    // a function is a sequence of steps to be exectuted in order
    // common uses: build an algorithm
    // procedure that encapsulates logic
    // reusable code basically

    // behings with fn -> pub fn
    // all snake cases
}
