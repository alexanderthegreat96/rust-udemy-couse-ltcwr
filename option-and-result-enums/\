#[allow(dead_code)]
#[allow(unused_variables)]
// optionals are as they sound
// thse are enums essentially
// they are basically Optional Values
// basically something that can be returned as a value or as None
fn main() {
    let a = Option::Some(5);
    let b = Option::Some("hello");
    let c = Option::Some(true);

    let a: Option<i8> = Option::Some(5);
    let a = Option::<i16>::Some(5);

    let d: Option<bool> = Option::None;

    if d.is_none() {
        println!("D is none");
    }

    // Option::None -> absense
    // Option::Some -> present value

    println!("{:?}", larget_than_two(6));
    println!("{:?}", larget_than_two(1));

    // the entire goal is to check if a value is present
}

fn larget_than_two(v: i32) -> Option<i32> {
    if v > 2 {
        return Some(v);
    }
    None
}
